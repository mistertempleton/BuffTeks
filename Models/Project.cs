using System;

namespace BuffTeks.Models
{
    public class Project
    {
        // attribute list
        // PK
        // name
        // leader = FK team lead
        // client = FK client
        // deadline - (Formatted as DateTime in C#)
    }
}
